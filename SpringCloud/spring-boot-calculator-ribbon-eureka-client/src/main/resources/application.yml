logging:
  pattern:
    level: debug
server:
  port: 8090

spring:
  application:
    name: calculator-eureka-client

eureka:
    client:
        register-with-eureka: true
        service-url:
            defaultZone: http://localhost:8761/eureka
 
